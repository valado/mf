const {
  SHARED_LIBS,
  SHELL_SHARED_SCOPE,
  getSharedLibsConfig,
} = require('./mfed-utils');
const { convert2ModuleName } = require('./utils');

module.exports = {
  SHARED_LIBS,
  SHELL_SHARED_SCOPE,
  getSharedLibsConfig,
  convert2ModuleName,
};
